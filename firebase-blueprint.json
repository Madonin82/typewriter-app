{
  "entities": {
    "UserProfile": {
      "title": "User Profile & Library",
      "description": "Stores user books, manuscript pages, ink streams, and studio settings",
      "type": "object",
      "properties": {
        "books": {
          "type": "array",
          "description": "Array of books with pages and committed ink chunks"
        },
        "settings": {
          "type": "object",
          "description": "Studio settings such as typography, sound effects, page mechanics, and theme"
        },
        "lastSynced": {
          "type": "string",
          "description": "Server timestamp of last sync"
        }
      }
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "UserProfile",
      "description": "User manuscript library and studio preferences"
    }
  }
}
