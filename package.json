{
  "name": "typewriter-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "echo 'No build step required for static server'",
    "lint": "node --check server.js && node --check app.js"
  },
  "devDependencies": {
    "sharp": "^0.35.4"
  },
  "dependencies": {
    "jspdf": "^4.2.1",
    "jszip": "^3.10.1"
  }
}
